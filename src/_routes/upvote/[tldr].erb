---<%
filename = params[:tldr]
user_ip = request.ip

Feedback.create(
  filename: filename,
  ip_address: user_ip,
  vote_type: "upvote",
  created_at: Time.now,
  updated_at: Time.now
)
%>---

<%= "Filename = #{filename} ip_address = #{user_ip}" %>

