---<%
filename = params[:tldr]
user_ip = request.ip
vote_type = params[:vote_type]

Feedback.update_or_create(
  { filename: params[:tldr], ip_address: request.ip },
  { vote_type: params[:vote_type], updated_at: Time.now }
)
%>---

<%= "Filename = #{filename} ip_address = #{user_ip}" %>

